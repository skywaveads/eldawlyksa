module.exports = {
  // These are the locales that are supported
  locales: ['ar'],
  
  // The default locale is Arabic (RTL)
  defaultLocale: 'ar',

  // Locale metadata for proper layout direction
  localeMetadata: {
    ar: {
      direction: 'rtl',
    },
  },
}; 